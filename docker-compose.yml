version: "3.9"

services:
  backend:
    build: ./backend
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - ADMIN_PIN=${ADMIN_PIN}
      - PORT=4000
    ports:
      - "4000:4000"

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
